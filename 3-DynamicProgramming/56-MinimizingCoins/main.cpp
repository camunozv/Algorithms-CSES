#include <bits/stdc++.h>

using namespace std;

int min_coin(int &x, vector<int> &coins, vector<int> &value, vector<bool> &calculated);

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n = 0;
    int x = 0;

    cin >> n;
    cin >> x;

    vector<int> coins(n);

    for (int i = 0; i < n; i++) {
        cin >> coins[i];
    }

    return 0;
}

int min_coin(int &x, vector<int> &coins, vector<int> &value, vector<bool> &calculated) {

    return 0;
}